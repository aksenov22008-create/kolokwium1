public record BoundingBox(double x, double y,
                          double width, double height) {

    /*
     * record
     * короткий клас для зберігання даних.
     *
     * Java автоматично створює:
     * - конструктор
     * - getter-и
     * - toString()
     */
}